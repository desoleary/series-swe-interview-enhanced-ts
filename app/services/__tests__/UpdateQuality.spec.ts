import UpdateQuality from '../UpdateQuality'

describe('UpdateQuality', () => {
  describe('run', () => {
    it('does something useful', () => {
      const instance = new UpdateQuality()
      instance.run()
    })
  })
})
